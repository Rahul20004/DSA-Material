class Solution {
    public int findNumbers(int[] nums) {
        // Initialize counter
        int count = 0;
        // Step 2: Iterate through the array 
        for(int num : nums){
            String str = String.valueOf(num);
            if(str.length() % 2 == 0){
                count++;
            }

        }
        return count;

        
    }
}


Explaination :
 Algorithm:
🛠️ Step 1: Initialize Counter
Create a counter count to store the number of valid elements.
🛠️ Step 2: Iterate Through Array
Loop through each element in nums.
For each number:
Convert to String:
Convert the number to a string using String.valueOf(num).
Check Length:
Get the length of the string using .length().
Check If Even:
If the length is even, increment the count.
🛠️ Step 3: Return Result
Return the count as the final answer.
